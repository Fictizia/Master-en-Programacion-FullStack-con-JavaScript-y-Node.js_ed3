### Principales

- `For` (num)
- `While` (cond)

### Grupos

- `While`
	- `Do... While`
- `For`
	- `for... in` (iterar propiedades de un obj)
	- `for... of` (iterar valores de un obj)
	- `forEach` (iterar elementos de un array)
		- `map` (itera generando un nuevo array)
			- `filter` 
		- `every`
		- `some`
		- `find`
		- `indexOf`
		- `reduce`
		- `reduceRight`
